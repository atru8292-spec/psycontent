# CLAUDE.md — компас проекта PsyCont

Это краткий справочник по проекту. Читать в начале каждой сессии. Держать курс по нему, не вспоминать из переписки.

---

## Продукт

PsyCont (не PsyContent) — AI-сервис для русскоязычных психологов: генерация контента для блога (посты, карусели, Reels-сценарии, хуки, контент-план) в голосе пользователя, чтобы привлекать клиентов.

Центр продукта: «звучит как ты». Учимся на голосе пользователя, пишем как живой психолог, а не как нейросеть. Это не одна из фич, а главный ответ на главную боль рынка (генерический AI-текст не цепляет).

## Позиционирование (актуальное, пересмотрено 26 июня)

Старое «блог без выгорания» (боль усталости) больше НЕ главное.

Новая главная боль острее: психолог хочет клиентов через блог, пробовал вести, но ничего не залетает и клиенты не приходят. Это не про усталость, а про бессилие — «не понимаю, что делаю не так».

Продаём НЕ как «сэкономит силы», а как «наконец сработает и приведёт клиентов».

Целевой человек для всего копирайта: психолог 2-5 лет практики, пробовал вести блог, постил без отклика, клиентов из блога нет. Писать для него одного.

Дифференциатор: PsyCont пишет как живой психолог, а не как очередная нейросеть (нейротекст видно, он не залетает). Подавать через результат («звучит как живой человек, поэтому работает»), не через комфорт.

North Star: число психологов, публикующих 4+ поста в месяц. Цель выручки: 100к руб/мес (~33 платящих).

---

## Стек (по факту, не по старому ТЗ)

- База и Auth: Supabase (project ID gzzsrzxavteafapdntgu). На Yandex Postgres НЕ переезжаем сейчас (дорого, юзеров нет). К запуску — перенос БД с персданными на дешёвый РФ-хостинг (Timeweb) для 152-ФЗ.
- AI: прямой OpenAI, модель GPT-5.4 на всех генерациях. OpenRouter выпилен, НЕ использовать.
- Фреймворк: Next.js.
- Сервер: VPS в Германии (Xorek), IP 31.77.197.158.
- Авторизация: email + Яндекс ID. Google убран (закон РФ). VK/Telegram позже.

## Сервер и деплой

КРАСНОЕ ПРАВИЛО: на сервере рядом работает чужой проект Razbor. Не трогать его процессы (razbor-bot/scraper/web/worker), его порты (3000, 8080), его nginx-блок, его SSL-сертификаты. Сервер НЕ перезагружать (уронит Razbor).

PsyCont: папка /var/www/psycont, порт 3001, процесс pm2 «psycont», свой nginx-блок на psycont.ru, свой SSL (Let's Encrypt, до 24 сентября, автопродление).

Обновление боевого сайта после пуша: зайти на сервер и выполнить `bash /var/www/psycont/update.sh` (сам подтягивает код, ставит зависимости, пересобирает, перезапускает только psycont; при ошибке сборки сайт остаётся на старой рабочей версии).

---

## Бренд

Палитра: крем/бумага #F7F3EC, индиго #2E2A45, аметист #5B4FA0, шалфей #8F9D68, лаванда #E7E2F2. Красный и кричащие цвета не использовать.

Шрифт: Onest (на всё). Lora+Manrope из старого паспорта заменены.

Лого: готовые файлы в public/logo (wordmark, icon, mono и т.д.).

Убрать из кода: старый неправильный фиолетовый #7C5CFC (заменить на аметист #5B4FA0), шрифт Inter (заменить на Onest). Переименовать PsyContent → PsyCont везде.

Визуальный язык: тёплые зернистые текстуры, винтажные ч/б коллажи с акцентами шалфея, рукописные подчёркивания, скруглённые формы.

Тон интерфейса и текстов: тёплый, человеческий, от боли и результата. Без давления и urgency. Без тире. Без анти-слоп клише. Слово «энергия», не «кредиты».

## PDF и выгрузки

Все PDF-экспорты и выгружаемые документы (бренд-паспорт, контент-план, карусели, лид-магниты, гайды) делаются в фирменном стиле: фон-бумага #F7F3EC, аметист #5B4FA0 для заголовков, шрифт Onest, лого PsyCont на обложке и в подвале, зелёные рукописные подчёркивания, скруглённые блоки, лавандовые плашки. Внизу каждого PDF подпись «Сделано в PsyCont» со ссылкой на psycont.ru. Образец: psycont-brand-passport.pdf. Безликие PDF не выпускать.

---

## Контент (генерация)

Все генерации сверять с рабочими документами: ЯДРО-ФОРМАТОВ (главный), БАНК-ФОРМАТОВ, ПРАВИЛА-ХУКОВ, ПРОМПТЫ-ГЕНЕРАЦИИ.

Что в документах:
- ЯДРО-ФОРМАТОВ: демо-психолог, типы хуков, структуры форматов, роли постов.
- БАНК-ФОРМАТОВ: структура каждого формата по блокам, когда использовать, под какую цель (охват/доверие/запись), на какой сигнал работает (досмотр/пересылка/сохранение).
- ПРАВИЛА-ХУКОВ: типы хуков с механикой, выбор под тему и площадку (рилс и ТГ хуки разные).
- ПРОМПТЫ-ГЕНЕРАЦИИ: промпт-схемы для движка.

Топ рабочих хуков (из разбора 71 рилса): цитата внутреннего голоса зрителя (зеркало, не диагноз), антихук, адресация по ярлыку-идентичности, незакрытый гештальт, признание беспомощности/антипродажа.

ГЛАВНОЕ правило генерации (не нарушать): продукт НЕ выдаёт готовые фразы из разборов. Живые формулировки — только как принципы и паттерны для few-shot, не клонировать. Иначе у всех психологов одинаковый контент.

Правила текста: показывать, а не анонсировать тему; писать для одного человека; хук сразу сценой. НЕ использовать «это не про X, это про Y», приклеенные концовки, английские кальки, объявление темы вместо крючка. Без тире. Заголовки до 6 слов.

CTA: НЕ в каждом посте. Большинство постов — чистая ценность без призыва. Роли постов в пропорции охват/доверие/запись примерно 60-70/20-30/10.

Хук решает половину: первая строка / первые 2-3 секунды / первый слайд цепляют через узнавание. Хук искренний, не фальшивый.

---

## Деньги (энергия и тарифы)

Текст безлимит на всех платных (скрытый честный потолок ~300/мес, пользователю не виден). Энергией метрится только дорогое: AI-картинки к каруселям, транскрибация, глубокий анализ конкурента.

Тарифы. Технический code НЕ трогать, меняются только видимые имена:
- code free → «Старт», 0 ₽ (бесплатный вход, 10 проб текста, разовые пробы: 1 карусель со своими фото, 1 проба AI-картинки, 1 проба анализа)
- code start → «Блог», 990 ₽ (текст безлимит, немного энергии, анализа нет → «доступно на Практике»)
- code practice → «Практика», 2490 ₽ (популярный, текст безлимит, больше энергии, глубокий анализ есть)
- code expert → «Студия», 4990 ₽ (всё, много энергии)
- code test → «Тест», 0 ₽ (служебный, не на витрине; безлимит, ничего не списывает, но в usage_log пишет would_charge — реальную себестоимость)

Принцип безопасности денег: пользователь только ЧИТАЕТ свой баланс/историю/тариф. Менять энергию может только сервер. usage_log и счётчик текста скрыты (только сервер). Списание атомарное (блокировка строки), в минус не уходит, при неудаче внешнего сервиса энергию не берём, журнал пишется всегда.

Энергия: месячная по тарифу сгорает, докупленная (когда будет оплата) копится отдельно. Числа энергии черновые — калибруются по реальной себестоимости (учёт токенов из ответов OpenAI пишется в usage_log).

Платёжку (ЮKassa) пока НЕ подключаем. Тариф «Тест» включается вручную через SQL по email.

---

## Обезличивание (152-ФЗ)

Перед отправкой в OpenAI личные данные маскируются (имена психолога и клиентов, телефоны, почты, ники, ссылки, города). Подключено во всех точках, где текст идёт в AI (включая паспорт и анализ конкурента).

Сейчас: JS-правила (lib/anonymize.ts) — широкий словарь имён, маркеры, фамилии по суффиксам, отчества, контакты, имя психолога из профиля гарантированно. Предел: редкие имена клиентов в свободном тексте без маркера просочатся.

ОБЯЗАТЕЛЬНО к запуску: spaCy (ru_core_news_lg) для надёжного распознавания. На живых психологах с реальными клиентами на одних JS-правилах НЕ запускаться.

## Защита от ботов (к запуску)

Перед запуском для реальных пользователей поставить защиту от ботов: российский антибот (DDoS-Guard) или капча на регистрации. Не пускать данные через западные сервисы (Cloudflare) из-за 152-ФЗ. Сейчас не ставим, пользователей нет.

---

## Тех-ловушки и ошибки (не повторять)

ПРАВИЛО САМООБУЧЕНИЯ (обязательное): каждый раз, когда находишь и чинишь баг или натыкаешься на грабли, СРАЗУ дописываешь сюда строку (что было, как избегать). Это часть фикса, не отдельная задача. Так файл растёт и ошибки не повторяются.

- Три роута используют `supabaseAdmin` вместо `supabase`: generate-content-plan, research-topics, generate-passport. При рефакторинге не путать.
- Защита по user_id обязательна на всех таблицах с польз. данными (на Supabase это RLS).
- Виджет выбора модели на экранах — мёртвый (сервер всегда GPT-5.4), удалить визуально на шаге бренда/UI.
- Не описывать продукт перечнем фич — от боли и результата. Не сужать до одного типа психолога (оба полюса тона через пресеты). Не воевать с курсами агрессивно.
- Фейковые отзывы на лендинге — убрать, не возвращать до реальных.
- Стриминг OpenAI: ручной по-чанковый декод тела (getReader + TextDecoder) живёт ТОЛЬКО в lib/sse-stream.ts (один TextDecoder со stream:true + буфер незавершённой строки). В роутах свой декод не плодить, иначе кириллица бьётся на стыках чанков. Стримят generate-passport и analyze-competitor — через iterateSSEData.
- Колонки профиля: читать реальные имена из onboarding_profiles (full_name, one_niche/niches, client_avatar/client_job, tone_verbal, client_pain_phrases). НЕ выдумывать profile.name/niche/client_pains — был баг в контент-плане (генерил по заглушкам мимо профиля).
- Автосоздания профиля при входе нет. Защита от Profile not found: guard в dashboard/layout.tsx (нет профиля → редирект в онбординг) + три роута (generate-passport, research-topics, generate-content-plan) отдают мягкий need_onboarding вместо сырого 404. Сырой текст ошибки юзеру не показывать.
- Профиль в роутах: РАЗЛИЧАТЬ «нет строки» (error.code === 'PGRST116') от ошибки чтения (права/сеть/сломанный service_role). Только PGRST116 → need_onboarding; любая другая ошибка → server_error 500 (мягкая плашка ServerErrorNotice + повтор), НИКОГДА не врать «заполните профиль» из-за серверного сбоя. Был замкнутый цикл онбординг↔контент-план, когда сломанный SUPABASE_SERVICE_ROLE_KEY на проде слепил admin-чтение, и роут ложно отдавал need_onboarding. Онбординг при наличии профиля НЕ делает слепой auto-push в дашборд (экран «профиль уже есть») — анти-цикл.
- ДИАГНОСТИКА (вход отвалился разом): если на проде не пускает и через Яндекс, и через почту, а Яндекс редиректит на `?error=yandex_link` — ПЕРВЫМ делом проверить, не приостановлен ли Supabase-проект (free-tier засыпает после простоя), а не лезть в `.env`/код. Без живого Supabase падает весь auth: Яндекс на admin.auth.admin.generateLink (отдает yandex_link), почта на verifyOtp. Лечится resume проекта в Supabase, код не трогать.
- СДЕЛАНО (кусок 3 волны входа): клиентские guard в dashboard/layout.tsx, dashboard/page.tsx, post-generator различают PGRST116 (нет профиля → /onboarding/express) от сбоя чтения (не выкидывают, layout показывает мягкий повтор). handleSubmit полного онбординга переведен на `.upsert({ onConflict: 'user_id' })` (не падает на дубле, полная распаковка дополняет строку экспресса).
- PDF только вектор (jsPDF, lib/brand-pdf.ts), Onest вшит с кириллицей. НЕ возвращать скриншот-подход (html2canvas) — он раздувал файл до 244 МБ и резал текст.
- Источник правды по ТЗ — только ТЗ-CLAUDE-CODE-АКТУАЛЬНОЕ.md. Старое ТЗ (в корне или в _знания/архив) не читать.
- Файл lib/openrouter.ts назван исторически, но ходит в прямой OpenAI. Не путать — это OpenAI-клиент. Не переименовывать (сломаются импорты), пояснение в шапке файла. Модель жестко одна (DEFAULT_MODEL=gpt-5.4), задается там же.
- СДЕЛАНО (чистка хвоста): мертвый дефолт preferred_model 'anthropic/claude-sonnet-4-5' вычищен из генераторов (generate-hooks/carousel/reels/content-plan/rewrite-post/carousel-topics). Там `const model = ...` и фетч user_settings были мертвые (generateWithAI игнорит переданную модель, берет DEFAULT_MODEL). Поле preferred_model в user_settings не читается нигде живьем. В analyze-competitor свой model='gpt-5.4' (используется), userSettings занулен через void — не трогали.
- Промпты генерации контента (post, carousel, reels, hooks, content-plan, rewrite) пишут от ПЕРВОГО лица психолога (он автор, не герой). Не упоминать его в третьем лице. Это правило только для генерируемого КОНТЕНТА, не для текстов интерфейса (там продукт обращается к психологу).
- ОТКРЫТАЯ ЗАДАЧА (не сейчас): на серийной генерации метафоры из approach-контекстов (getApproachContext в generate-post) приедаются (напр. «прерванный жест» в 3 из 4 постов). Разнообразить approach-контексты или ротировать метафоры.
- ХВОСТ (отдельным заходом): поменять метку CTA на «Призыв/Финал» в carousel (СЛАЙД 10) и rewrite-post (примеры и структура), по аналогии с рилсом (там уже сделано). Калька CTA, а не лексика психолога.
- ХВОСТ (отдельным заходом): продукт несогласован по тону. Онбординг и старые экраны на «вы», новые куски волны входа на «ты» (совпадает с ядром «звучит как ты» и доком ВХОД-И-ОНБОРДИНГ.md). Привести весь видимый интерфейс к одному тону (вероятно «ты»). В куске 1 волны входа новые тексты на «ты».
- ЭКСПРЕСС-ОНБОРДИНГ (кусок 3 волны входа, сделано): новый роут /onboarding/express, 5 критичных полей (approaches, tone_verbal, client_pain_phrases, full_name, one_niche) — выбраны по факту использования в промпте generate-post (approaches тянет getApproachContext, tone_verbal несет идиолект, client_pain для узнавания). Регистрация ведет на экспресс, полный онбординг /onboarding остается как «настроить голос точнее» (апселл после первого поста). Метки чипов подхода = ровно ключи getApproachContext. ГОЛОСОВОЙ ВВОД В ЭКСПРЕССЕ — заглушка «скоро»: /api/transcribe это Supadata-YouTube (по URL), не транскрибация записанного аудио. Реальный голос нужен свой Whisper (к запуску, см. раздел обезличивания).
- ХВОСТ (продуктовый, следующими кусками): перехват демо на лендинге упирается в 22 вопроса онбординга между мыслью и постом. Демо заманивает (вложил мысль), а онбординг отпугивает (анкета на 25 мин до награды). Аргумент укоротить онбординг (экспресс-уровень из аудита/документа) или дать пост ДО онбординга (раздел 6 ВХОД-И-ОНБОРДИНГ.md). Сейчас перехват честно говорит «сначала знакомство, потом пост», но это полумера.
- ФАКТ к решению про паспорт/козырь: паспорт идёт в генерацию обрезанным до 800 символов из ~27760 (3%), голос держится на профиле (live_voice, tone_verbal и т.д., целиком). Паспорт для генерации сейчас почти декоративный. Учесть при работе над «звучит как ты» (обучение на постах = отдельная волна).
- ЗАГОЛОВОК ПОСТА (ЭТАП 1 плана онбординга, сделано): пост раньше выходил сплошным текстом без заголовка. Промпт generate-post теперь даёт строгий структурный вывод для format='post' (первая строка заголовок-вывеска ≤6 слов, ОБЯЗАТЕЛЬНО пустая строка, потом тело со сценой-хуком; блок ЗАГОЛОВОК разводит вывеску и хук, блок РЕЗУЛЬТАТ требует пустую строку, без markdown/звездочек чтобы копия в инсту была чистой; примеров заголовков НЕ даём чтобы не клонировались). Разбор на title/body — общий хелпер lib/post-format.ts splitPostTitle(content, format): только для post, режет по первой пустой строке, заголовок принимается лишь если выглядит как вывеска (≤7 слов буфер, ≤70 символов, нет терминальной пунктуации, не начинается с кавычки/скобки/[Экран). Старые посты и сторис → title=null, рендер как раньше. Рендер заголовка (h3 жирный индиго + Squiggle шалфея, тело brand-text-secondary) в post-generator и post-history, шапку автора и футер-иконки из демо в кабинет НЕ тащим. Парсить по формату ГЕНЕРАЦИИ (generatedFormat), не по живому селектору. Заголовок не колонка в БД, хранится в content первой строкой.
- КАБИНЕТ (ЭТАП 2 плана онбординга, сделано): /dashboard/settings превращен из узкого «Тариф и энергия» в хаб «Настройки» с секциями Голос/Тариф/Энергия (тон на «ты»). EnergyInfo (иконка «?» + поповер «почему почти все бесплатно») рядом с EnergyBadge в hero. TariffSection это ВИТРИНА (components/TariffSection.tsx): показывает 4 тарифа, текущий «Ты здесь», кнопка «Выбрать» → поповер «Оплата скоро». КРИТИЧНО: ничего не пишет в БД и не меняет план (план меняет только сервер). Поповер НЕ обещает рассылку (механизма сохранения email нет), текст «загляни сюда чуть позже». Вход в правку профиля ведет в существующий edit-profile. Карточка «настроить голос точнее» для экспресс-юзера: lib/profile-status.ts isVoiceIncomplete = пусты live_voice И values; в настройках всегда, на главной только после первого поста (stats.posts>0).
- ИНВЕРСИЯ ТОНА (важный фикс): метки бегунков тона в onboarding и edit-profile были ПЕРЕВЕРНУТЫ относительно движка lib/profile-context.ts. Движок читает высокое значение tone_formal/serious/cautious как формальный/серьезный/осторожный, а слева стояло «Формальный» (= низкое значение) — двигаешь к «Формальный», в промпте выходит «дружеский». Выправлены метки: left=Разговорный/С юмором/Прямой, right=Формальный/Серьезный/Осторожный. Движок (profile-context) НЕ трогали. Бегунки добавлены и в экспресс (свернуты под «тонкая настройка тона», дефолт 50). ВАЖНО: при чтении/сохранении тона использовать `?? 50`, НЕ `|| 50` (0 это валидный край «максимально разговорный», `0 || 50` его терял).
- ЛИМИТ ТЕКСТА FREE = 10 (plans.fair_use_text_cap в seed-миграции 20260625140000), не 5. getAccountSummary отдает textTrial.cap=10, энергопанель и витрина тарифа показывают «из 10». Витрина тарифа («10 пробных текстов») должна совпадать с этим числом. Если менять лимит, это правка seed/SQL (деньги, через Арину), а не текста в UI.
- ЭНЕРГИЯ ГЛОБАЛЬНАЯ (через layout): EnergyBadge + EnergyInfo («?») живут в app/dashboard/layout.tsx (низ десктоп-сайдбара + мобильная шапка), поэтому видны на ВСЕХ страницах кабинета. НЕ добавлять бейдж на отдельные страницы руками (был дубль на hero главной, убран). /api/me тянется один раз в layout (useState energy) и отдается обоим бейджам через проп data, чтобы не было двойного запроса. EnergyBadge compact = иконка+значение (шапка/свернутый сайдбар), без compact = полный. EnergyInfo placement: 'header' (мобилка, поповер fixed в правом верхнем углу) или 'sidebar' (десктоп, поповер открывается вверх и вправо) — иначе поповер клипается за край (триггер у низа/не у правого края). Ссылки энергии ведут на /dashboard/settings#energy (якорь секции есть).
- ЛИСТАЛКА-ЗНАКОМСТВО (ЭТАП 3 плана онбординга, сделано): новый экран app/onboarding/intro/page.tsx, 6 слайдов (боли психолога из ресерча: тишина/язык клиента/не маркетолог/продажи неловко/форматы/старт), ручное листание (свайп + стрелки + клик по точкам), Пропустить всегда, последний слайд → express. Мотивы из брендовых примитивов, анимация opacity+x, reduced-motion. Поток: регистрация → /onboarding/intro → express → первый пост. МАРШРУТИЗАЦИЯ новых юзеров переведена на интро: AuthModal (email signup/signin без профиля), app/auth/yandex/callback (dest), app/auth/callback (next дефолт). Guard листалки: нет сессии → '/', есть профиль → '/dashboard', есть мысль из демо (psycont_seed_thought в localStorage) → сразу '/onboarding/express' (листалку видит ТОЛЬКО холодный вход, демо-юзер минует, чтобы не отодвигать награду). Хоткей Enter обрабатывать только если фокус НЕ на кнопке (иначе клик+Enter проскакивают 2 слайда).
- ПРЕДЛОЖЕНИЕ РАЗБОРА (ЭТАП 4 плана онбординга, сделано): после первого поста в post-generator (firstMode) показывается ПРИЛИПАЮЩАЯ ПОЛОСА внизу экрана (sticky bottom bar, не карточка под постом — та пряталась внизу длинного поста, человек не долистывал). Условие showVoiceBar: result && !generating && firstMode && isVoiceIncomplete(profile) && !voiceOfferDismissed (только экспресс-юзеру с пустыми live_voice/values). Полоса fixed inset-x-0 bottom-0 z-40, AnimatePresence slide-up (delay 0.6 на вход, exit БЕЗ delay иначе кажется что не сработала), на контент условный pb-40 sm:pb-24 чтобы низ поста не уходил под полосу. На мобилке плашка full-bleed (px-0), кнопка w-full, ссылка «Позже»; десктоп — крестик. Текст: «Расскажи о себе, и посты станут точнее» + микроподпись «Минут десять, и живее обычной анкеты». Кнопка «Рассказать о себе» → /dashboard/edit-profile (НЕ /onboarding, там dead-end «профиль уже заполнен» у юзера с профилем). Старый зов «Настроить голос → /onboarding» из хвоста firstMode убран. Закрытие (X/«Позже») → voiceOfferDismissed на сессию, при «написать еще пост» НЕ сбрасывается. Тексты через copywriter-psycont (заголовок «Этот пост, только ближе к тебе» был калькой, переписан). Размещение через designer-psycont + ux-скилл. Сам полный разбор (архетип, бегунки) это ЭТАП 5.
- ЭТАП 4 ДОВОДКА (сделано): (а) Частота полосы: отвязана от firstMode (был баг — внутри сессии не возвращалась, каждую новую сессию долбила). Счетчик psycont_post_count + точка отказа psycont_voice_offer_dismiss в localStorage. showVoiceBar = result && !generating && isVoiceIncomplete && (voiceDismissAt==null ? postCount>=1 : postCount-voiceDismissAt>=3): после «Позже» пауза ~3 поста, и так пока профиль неполный. Текст переписан (был «опять анкета»): «Это было знакомство на бегу. Теперь копнем глубже», кнопка «Копнуть глубже». (б) РЕЖИМ DEEPEN: edit-profile читает ?deepen=1 (window.location.search), DEEPEN_SKIP = approaches/niches/tone_verbal/tone_sliders/client_pain_phrases, visibleQuestions фильтрует, прогресс/навигация/сабмит по visibleQuestions (21 из 26). Вход через предложение и кабинетные «дополнить» → ?deepen=1; «редактировать профиль» (полный, из настроек) → без параметра. Данные скрытых вопросов не теряются (форма префилит, .update пишет весь объект). Плашка «Режим редактирования» убрана совсем.
- МОБИЛЬНОЕ ПРАВИЛО (применять везде): заголовок шага и главное действие (кнопка) видны на первом экране мобилки без долгой прокрутки. Паттерн для визардов: корень h-[100dvh] flex flex-col, тело flex-1 min-h-0 overflow-y-auto (центр через my-auto на контенте), панель действий shrink-0 последним ребенком (кнопки всегда внизу). Применено в листалке intro (иллюстрация на мобилке max-h-[30dvh] scale-[0.58], текст компактнее). onboarding и edit-profile уже с fixed-bottom баром. express это обычная скролл-форма (submit в конце, норма).
- ХВОСТ (отдельными заходами): (1) tone_verbal хранит ДВА формата — живой текст из экспресса и пресет из формы edit-profile; в deepen пресет не показываем (защита), но в обычном «редактировать» можно случайно затереть живой голос пресетом, развести поля. (2) Тон «вы» в edit-profile/онбординге vs «ты» во входе — свести к «ты» (deepen делает стык ты→вы заметным). (3) Предсуществующий emoji-слоп: «🎠 Нужна карусель?» (post-generator), «⭐ Избранное» (post-history) — вычистить.
- АРХЕТИП ВО ВСЕХ ГЕНЕРАТОРАХ (ЭТАП 5.4, сделано): архетип автора (тест-распаковка) раньше кормил почерк только generate-post. Теперь блок «АВТОРСКИЙ ПОЧЕРК» вставляется во ВСЕ генераторы контента (carousel/reels/hooks тоже). Единый хелпер lib/archetypes.ts getArchetypeContextFromProfile(profile): берет profile.archetype_scores.selection, иначе денормализованный profile.archetype_primary как {primary}, иначе '' (нет архетипа -> блока нет, промпт как раньше, регрессии нет). generate-post отрефакторен с инлайна на этот хелпер. Размещение блока: carousel/hooks после паспорта перед ЗАДАНИЕМ, reels после блока СТИЛЬ ПОДХОДА перед ЗАДАНИЕМ, обёрточный паттерн `${archetypeContext ? ... : ''}` одинаков везде. Приемка: одна тема, Мудрец даст «разбор механики, спокойно, глубже», Шут «о сложном легко, самоирония, разрыв шаблона» — блок реально меняется по архетипу.
- ХВОСТ (от ревью 5.4, отдельными заходами): (1) логика чтения архетипа (archetype_scores.selection ?? {primary: archetype_primary}) дублируется в settings/page.tsx и archetype-story/route.ts — при случае перевести серверный archetype-story на getArchetypeContextFromProfile (settings клиентский, свой импорт). (2) В generate-reels buildProfileContext вызывается БЕЗ try/catch (в generate-post обёрнут с мягким фолбэком) — при кривом профиле reels отдаст 500, post деградирует мягко. Свести поведение генераторов.
- ТОЧКИ ВХОДА В ТЕСТ (ЭТАП 5.5 шаг 1, сделано): три приглашения «настроить голос» (полоса после поста в post-generator, карточка на главной, карточка в настройках) ведут в тест-архетип /onboarding/archetype, НЕ в старую анкету. Сигнал полноты lib/profile-status.ts переименован isVoiceIncomplete -> isArchetypeIncomplete и смотрит на пройденный тест (пусты archetype_scores.selection.primary И archetype_primary), приглашение гаснет ровно после теста. В настройках секция «Голос» свернута в «Профиль практики» (edit-profile без deepen), вход в тест там это существующая секция «Твой архетип» (без дубля на экране).
- ТРИ СУЩНОСТИ ПРОФИЛЯ (ЭТАП 5.5 шаг 2, сделано). (1) ТЕСТ-АРХЕТИП = стиль/личность (тон, ценности, суперсилы, живой голос, история). (2) ПРОФИЛЬ ПРАКТИКИ = факты, короткая форма edit-profile (12 экранов, 3 блока: «Ты и практика», «Твой клиент», «Как ты в блоге»): имя, подход, ниши+одна, стаж, форматы+цена, аватар+чем занимается, боль клиента, что пробовал, страх, результат, площадки+подписчики+тумблер видео, антиценности (чипы). (3) ЦЕЛИ = трекер (шаг 3, контент-план), из анкеты убраны. ВАЖНО про сохранность: edit-profile prefill и handleSubmit пишут ТОЛЬКО факты профиля практики, стилевые/целевые колонки (live_voice, values, superpowers, tone_*, цели) в .update НЕ входят намеренно, значит не затираются (результат теста и старой анкеты жив). Тумблер видео хранится строкой в существующем поле video_attitude ('Снимаю видео'/'Пока без видео'), схему БД не трогали, generate-passport читает осмысленно. Deepen-режим удален совсем. Голосовой ввод: компонент components/VoiceTextarea.tsx (textarea + микрофон-заглушка «скоро», паттерн из теста-архетипа) на всех развернутых полях (одна ниша, чем занимается, боль, результат), реальной транскрибации нет, только место под нее (свой Whisper к запуску). profile-context.ts почищен: из промпта генерации убраны content_struggles, content_pain, goal_3_months, dream_blog, idols.
- ТРЕКЕР РИТМА (ЭТАП 5.5 шаг 3, сделано, этап 5.5 закрыт). Слим-полоса «Твой ритм · этот месяц» в контент-плане (app/dashboard/content-plan/page.tsx, компонент RhythmTracker): считает материалы за текущий календарный месяц к цели 4+ (North Star). Источник generated_posts по created_at >= начало месяца. ВАЖНО про счет: generated_posts пишут 5 роутов, туда падают и хуки (format:'hooks') и рерайты (format:'rewrite_*') — их из счета ИСКЛЮЧАЕМ (.not format eq hooks + .not format like rewrite%), считаем только посты/сторис/карусели/рилс (это и есть «материал»). Считаем СОЗДАННОЕ (не подтверждаем публикацию), поэтому копия «собрано», не «опубликовано». Визуал: слим-полоса на лаванде bg-brand-soft с 4 сегментами-пилюлями к цели (min(count,4) закрашено аметистом), разведена с существующей карточкой «Прогресс» плана формой (сегменты vs заливка %) и фоном (лаванда vs белая), не геймификация. Три состояния 0/1-3/4+ (rhythmCaption), при 4+ значок Check «в ритме» и число растет дальше. Склонение материал/материала/материалов через pluralMaterial. Полоса видна и в пустом состоянии (решение Арины), но при N=0 подпись АМБИЕНТНАЯ без призыва («Месяц только начался, тут пока пусто...»), чтобы не конкурировать с кнопкой «Составить план». Скрыта при generating. МЕЛОЧЬ на потом: monthStart в локальной TZ, created_at в UTC — на стыке месяца счет может сдвинуться на пару записей, некритично.
- ГОЛОС РАБОТАЕТ (ЭТАП 6.1/6.2, сделано, на боевом). Свой Whisper (faster-whisper small, int8, ru) как HTTP-сервис на ОТДЕЛЬНОМ сервере Арины 64.188.119.94:8090 (папка /transcriber в репо, деплой руками, systemd psycont-transcriber, Bearer-токен + firewall на IP прода, аудио удаляется сразу, 152-ФЗ). НЕ на проде PsyCont (31.77.197.158, там Razbor). Мост: роут app/api/voice-transcribe (сессия обязательна, серверный токен клиенту не виден, env WHISPER_SERVICE_URL/WHISPER_SERVICE_TOKEN на проде). Фронт: хук lib/use-voice-recorder.ts (getUserMedia/MediaRecorder, авто-стоп 120с, «Повторить» с хранением Blob) + компоненты components/VoiceButton.tsx (VoiceButton кнопка в углу + VoiceStatus строка-статус), общие для VoiceTextarea (поля профиля практики) и открытых вопросов теста-архетипа. Текст добавляется В КОНЕЦ поля (appendVoiceText), не заменяет. Работает только на https (getUserMedia). 152-ФЗ галочка к запуску: сервер заграничный, аудио транзитом за рубеж, нужно проговорить в согласии/политике (аудио не хранится, только расшифровка). ОСТАЛОСЬ по этапу 6: голосовое интервью с ИИ (6.3) — распаковка монологом 2-3 мин -> паспорт, отдельным крупным заходом.
- ПАСПОРТ ПОЧИЩЕН (сделано, хвост от 5.5 шаг 2 закрыт): из промпта generate-passport убраны 15 мертвых полей, которые анкета больше не собирает (path_to_profession, values_custom, anti_values_custom, content_struggles, current_clients, client_source, content_pain, content_pain_detail, goal_3_months, desired_clients, time_available, dream_blog, idols, idols_why, something_else) — секции ЦЕЛИ и РЕФЕРЕНСЫ убраны целиком. video_attitude перенесен в секцию ГДЕ СЕЙЧАС. Добавлен блок «АВТОРСКИЙ ПОЧЕРК» через getArchetypeContextFromProfile (как во всех генераторах, этап 5.4) в секцию ГОЛОС И ХАРАКТЕР. Стилевые легаси (tone_*, tone_verbal, values, anti_values, superpowers, live_voice) оставлены (у старых юзеров ценны, у новых пусто). Обезличивание и обрезка не тронуты.

## Три папки на Mac (не путать)

- ~/Documents/psycontent — код/репозиторий (для Claude Code)
- ~/Documents/razbor-kontenta — контент-ресёрч с аналитическими агентами
- ~/Documents/PsyCont — библиотека документов

## Агенты (.claude/agents/) — ОБЯЗАТЕЛЬНЫЕ правила

Агентов подключаешь ТЫ сам, по типу задачи, не дожидаясь просьбы Арины. Арина — оркестратор, но рутинный выбор агента не должна делать руками каждый раз. Это не опционально.

- Любая визуальная/дизайнерская задача (вёрстка экрана, PDF, лендинг, компонент, бренд) → привлекаешь designer-psycont (opus, read-only) ДО начала для структуры/иерархии/состояний и ПОСЛЕ для честной оценки результата.
- Перед пушем любого кода → прогоняешь через reviewer-psycont (opus, read-only) — последний рубеж качества/безопасности/бренда.
- Продуктовое решение или новая фича → сверяешь с product-kritik (opus, read-only) на боль/ТЗ/ценности, ПЕРЕД и ПОСЛЕ реализации.
- Бэкенд по существу (API, Supabase, лимиты/энергия, безопасность) → backend-psycont (opus). Фронтенд по существу (экраны, компоненты, бренд-вёрстка) → frontend-psycont (opus).
- Текст ПРО продукт (лендинг, экраны, кнопки, слайды, письма, описания тарифов, микрокопи) → copywriter-psycont (opus, read-only). Продуктовый копирайтер, пишет человеческим языком на боли ЦА, НЕ блоговый контент. Опирается на живые посты психологов в _знания/живые-посты/ (сырые ТГ-посты для интонации, не для копирования).

Все шесть проверены и рабочие (модель указана и существует, инструкции на месте). Прочие агенты в папке (analitik-kontenta, analitik-profilya, strateg-psycont и т.д.) — для контент-ресёрча, не для разработки.

## Копирайтер и дизайн-скиллы — ОБЯЗАТЕЛЬНО

ПРОДУКТОВЫЕ ТЕКСТЫ: агент copywriter-psycont (opus) вызывать ВСЕГДА для текстов ПРО продукт — лендинг, экраны, кнопки, слайды, письма, тарифы, пустые состояния, ошибки, микрокопи. Человеческим языком на боли ЦА, обучен на живых ТГ-постах в _знания/живые-посты/. Продуктовые тексты НЕ писать из головы, гонять через него.

ДИЗАЙН-СКИЛЛЫ (.claude/skills/), перед визуальной/UX-задачей поднимать подходящий и применять, не на глаз:
- visual-web-designer — композиция страниц и лендингов, раскладки, типографика, тени, глубина, чтобы не слоп.
- frontend-design-pro — дизайн компонентов фронта.
- product-ui-web-app-design — UI кабинета, дашборда, форм, таблиц, настроек.
- ui-design-system — токены, цвета, типографика, консистентность.
- interaction-states-ux-writing — состояния (hover/focus/empty/error) и микрокопи.
- user-experience — UX-паттерны, удобство.
- ux-researcher-designer — потоки, онбординг, journey, где человек что ищет.
- ui-ux-pro-max — общий UI/UX справочник.
Правило: перед визуалом/UX поднять подходящий скилл и применять. Перед продуктовыми текстами — copywriter-psycont. designer-psycont остается для брендовой структуры и честной оценки результата.

---

## Живой язык (как убирать ИИ-шелуху) — суть дифференциатора

Продукт про «звучит как человек, а не как нейросеть». Это должна знать ЛЮБАЯ сессия, не только копирайтер. Детали и образцы живой речи — в агенте copywriter-psycont (.claude/agents/copywriter-psycont.md, раздел «Живой язык vs ИИ-литература»), тут кратко.

- Главный источник живого языка: транскрипты рилсов психологов в _знания/транскрипты-рилсов/ (устная речь с камеры, ее не подделать). Вторичный: живые ТГ-посты в _знания/живые-посты/. Сверяться с ними перед тем как писать любой текст (продуктовый или генерируемый).
- ЗАПРЕТ НОМЕР ОДИН: конструкция «не X, а Y» (и формы «это не про X, это про Y», «дело не в X, а в Y», «не просто X, а Y»). Самый частый признак нейросети. Всегда переписывать на прямое утверждение (не «дело не в лени, а в страхе», а «за откладыванием часто прячется страх»).
- Другие ИИ-маркеры: книжные абстракции и пафос («раскрыть потенциал», «безопасное пространство», «путь к себе», «обрести гармонию», «ресурсное состояние»), терапевтический жаргон в тексте для людей («остаюсь с ним в этом», «контакт с собой»), старомодные обороты («носило по волнам»), английские кальки, приклеенные концовки с моралью.
- Правило-тест перед каждой фразой: реальный психолог правда так скажет вслух в своем рилсе? Красивое, но абстрактное или книжное — переписать на конкретное и живое.

---

## Как работаем (процесс)

- Перед каждым шагом — план простыми словами, ждать «ок». Не начинать без подтверждения.
- После шага — коммит с понятным сообщением, пуш (после проверки).
- Деньги и безопасность — двойной контроль (показать SQL/доказательства до применения).
- Изменения базы — показать миграцию на просмотр, Арина применяет сама в Supabase.
- Развилку, которой нет в ТЗ, — спросить, не решать самому.
- После шага приносить наглядные доказательства (что проверено, с результатом).
- После каждого UI-шага проверять мобильную версию (320-390px): подчёркивание, фон, пустые состояния, лого, чтобы ничего не поехало. Мобилка в чеклисте каждой UI-волны.

## Предпочтения Арины (общение)

Русский, на «ты», женский род. Без тире. Без буквы ё (писать е) во всех видимых пользователю текстах и в промптах (в коде/regex/словарях ё не трогать). Максимум деталей, не сжимать. Одно конкретное решение вместо списка вариантов. Обсуждать находки до написания документов. Не подталкивать к аутричу и продажам, пока продукт не готов. Не вставлять в технические задачи комментарии про «нужно найти платящих».
